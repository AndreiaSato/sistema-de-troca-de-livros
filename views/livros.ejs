<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Troca de livros</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/css">
</head>
<body class="bg-info">
    <div> 
        <%- include('header.ejs') %>
    </div>
    <div class="container-fluid mb-3">
        <div class="row mt-5">
            <div class="col-12">
                <h1 class="text-center fw-bold mt-4 text-white">Livros disponíveis</h1>
            </div>
            <div class="col-2"></div>
            <div class="col-8">
                <div class="border border-4 shadow shadow-lg rounded p-0 m-0">
                    <table class="table table-bordered table-striped shadow shadow-lg m-0">
                        <thead class="thead-dark">
                            <tr>
                                <th class="bg-primary text-white">ID</th>
                                <th class="bg-primary text-white">Título</th>
                                <th class="bg-primary text-white">Autor</th>
                                <th class="bg-primary text-white">Condição</th>
                                <th class="bg-primary text-white">Descrição</th>
                                <th class="bg-primary text-white">Situação</th>
                                <th class="bg-primary text-white">ID do Usuário</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% obj_livros.forEach(livro => { %>
                                <tr>
                                    <td class="bg-secondary text-white fw-bold"><%= livro.idLivro %></td>
                                    <td class="bg-light fw-semibold"><%= livro.tituloLivro %></td>
                                    <td class="bg-light fw-semibold"><%= livro.autorLivro %></td>
                                    <td class="bg-light fw-semibold"><%= livro.condicaoLivro %></td>
                                    <td class="bg-light fw-semibold"><%= livro.descricaoLivro %></td>
                                    <td class="bg-light fw-semibold"><%= livro.situacaoLivro ? 'Disponível' : 'Indisponível' %></td>
                                    <td class="bg-light fw-semibold"><%= livro.fk_usuario_idUsuario %></td>
                                </tr>
                            <% }); %>
                        </tbody>
                    </table>
                </div>
            </div>
            <div class="col-2"></div>
        </div>
    </div>
</body>
</html>